{
  "name": "moment-lit-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "gray-matter": "^4.0.3",
    "marked": "^16.0.0",
    "sanitize-html": "^2.17.0",
    "typescript": "~5.9.3",
    "vite": "^7.1.0"
  }
}
